import React from 'react';
import './NavBar.css';

const NavBar = () => {
  return (
    <nav className="navbar">
      <h2 className="navbar-title">Tubestamp</h2>
    </nav>
  );
};

export default NavBar;